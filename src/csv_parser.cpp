#include <vector>
#include "../include/csv_parser.h"

using std::ios;
using std::vector;

CsvParser::CsvParser(string path)
{

  // this->file->open(ios::in);
}

vector<City *> CsvParser::ParseCities()
{

  vector<City *> cities;
}
